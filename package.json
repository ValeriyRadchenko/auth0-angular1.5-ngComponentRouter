{
  "name": "angular_auth0_component_router",
  "version": "1.0.0",
  "description": "An Angular 1.x example app using the component router and Auth0",
  "scripts": {
    "start": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Auth0",
  "dependencies": {
    "@angular/router": "^0.2.0",
    "angular": "^1.5.7"
  },
  "devDependencies": {
    "lite-server": "^2.2.2"
  }
}
